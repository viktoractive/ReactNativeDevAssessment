import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  dotsContainer: {
    borderRadius: 25,
    borderColor: 'black',
    borderWidth: 1,
    width: 50,
    height: 50,
    alignSelf: 'flex-end',
  },
  dots: {
    fontSize: 25,
    alignSelf: 'center',
  },
});

export default styles;
